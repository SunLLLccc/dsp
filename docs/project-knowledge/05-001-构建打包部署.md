# 构建打包部署

> 由 pm-build-deploy 自动生成。每条命令/部署方式/版本号附【来源】【标注】字段。
> 标注：已验证=配置文件中直接找到；推断=未直接找到/来自文档/需人工确认；未发现=探测后确认不存在。
> 本文档只梳理现状，不含顾问建议（推荐改造、建议流水线等不在本 skill 职责内）。
> 上下文说明：本文以 `01-001-项目概览.md` 为主、`02-001-技术栈与架构.md` 为辅；**02 尚未产出（文件不存在），故全篇以 01 + 配置文件实际读取为准**。

## 多构建并存声明

本项目为**多构建并存**：后端 Maven 多模块 + 前端 Vite/npm，两种构建独立成节梳理。

| 构建形态 | 模块/目录 | 配置文件 |
|----------|----------|----------|
| Maven（后端多模块，Spring Boot 2.7.18） | `dsp-parent/`（6 子模块） | `dsp-parent/pom.xml` + 6 子模块 `pom.xml` |
| Vite + npm（前端，Vue 3） | `dsp-admin-web/` | `dsp-admin-web/package.json` + `dsp-admin-web/vite.config.js` |

> 附：`dsp-parent/package-lock.json` 存在但 `packages: {}` 为空、且 `dsp-parent/` 下无 `package.json`，非真实 npm 工程产物（疑似误生成），不作为构建形态。

## 一、环境要求

### JDK（后端）

| 要求 | 版本 | 来源 | 标注 |
|------|------|------|------|
| JDK | 1.8 | `dsp-parent/pom.xml: <properties><java.version>1.8</java.version>` | 已验证 |
| JDK（CI 运行时） | 8（temurin） | `.github/workflows/ci.yml: actions/setup-java@v4 with java-version: '8'` | 已验证 |

> Maven 工具版本：项目**无 Maven Wrapper**（探测 `mvnw` / `mvnw.cmd` / `.mvn/` 均无），Maven 为系统级依赖，版本未在项目配置中声明。【来源】未发现 wrapper，【标注】推断 — 需外部安装 Maven 3.6+（README 第 43 行"环境要求"提及 3.6+，作推断线索，`README.md: 43`）。

### Node.js（前端）

| 要求 | 版本 | 来源 | 标注 |
|------|------|------|------|
| Node.js（CI 运行时） | 20 | `.github/workflows/ci.yml: actions/setup-node@v4 with node-version: '20'` | 已验证 |
| Node.js（本地开发） | 配置未声明 | `dsp-admin-web/package.json` 无 `engines` 字段；项目无 `.nvmrc` / `.node-version` | 推断（依据：配置未声明，仅 CI 锁定 20） |

> `dsp-admin-web/package.json: 1-23` 读完，无 `engines` 字段；项目根与 `dsp-admin-web/` 下均无 `.nvmrc` / `.node-version`。本地开发 Node 版本需人工确认（README 第 44 行提及"Node.js 16+"为推断线索）。

### 数据库 / 缓存 / 中间件

| 要求 | 版本 | 来源 | 标注 |
|------|------|------|------|
| MySQL | 5.7（CI） | `.github/workflows/ci.yml: services.mysql.image: mysql:5.7` | 已验证（CI 固定版本） |
| Redis | 配置未声明 | 三份 `application.yml` 仅 `${DSP_REDIS_HOST:localhost}` + `port: 6379`，无版本字段 | 推断（依据：README 第 45 行"Redis 5.0+"，配置未声明） |
| MongoDB | 配置未声明 | `application.yml: ${DSP_MONGO_URI:mongodb://localhost:27017/dsp}`，无版本字段 | 推断（依据：README 第 46 行"MongoDB 4.0+"，配置未声明） |

> MySQL 版本仅在 CI 配置中固定为 5.7（已验证），生产 MySQL 版本未在项目配置中声明，需人工确认。

## 二、依赖安装

### 后端 Maven

| 命令 | 来源 | 标注 |
|------|------|------|
| 无独立安装步骤，构建时自动解析 | `dsp-parent/pom.xml: <dependencyManagement>` + 各子模块 `pom.xml: <dependencies>`；由 Spring Boot 父 POM（`spring-boot-starter-parent:2.7.18`）统一版本管理 | 已验证 |

> Maven 项目依赖在 `mvn` 构建（compile/package）时从仓库自动拉取，版本由 `dsp-parent/pom.xml: 44-124` 的 `dependencyManagement` 与 `properties` 集中管理（`<dsp.version>1.0.0</dsp.version>` 等）。

### 前端 npm

| 命令 | 来源 | 标注 |
|------|------|------|
| `npm install` | `dsp-admin-web/package.json` 存在 + 锁文件 `dsp-admin-web/package-lock.json`（lockfileVersion 3，npm 锁定）；无 `pnpm-lock.yaml` / `yarn.lock` / `packageManager` 字段 | 已验证 |

## 三、开发启动

### 后端（dsp-parent，3 个可部署服务）

| 命令 | 来源 | 标注 | 备注 |
|------|------|------|------|
| IDE 直跑 `*Application.java` 主类 | 启动类路径：`dsp-data-service/src/main/java/com/sunlc/dsp/dataservice/DspDataServiceApplication.java`（`@SpringBootApplication`） | 推断（Spring Boot 标准启动方式，配置中无 `mvn spring-boot:run` goal 显式声明） | 3 个服务对应 3 个启动类：`DspDataServiceApplication` / `DspOfflineServiceApplication` / `DspAdminServiceApplication` |
| `mvn spring-boot:run -pl dsp-data-service` | 3 个服务模块 `pom.xml` 均含 `spring-boot-maven-plugin`（如 `dsp-data-service/pom.xml: 60-75`） | 推断（插件存在，goal 可用，但项目未在 scripts/文档中给出此命令字面量；README/CLAUDE 均推荐打包后 `java -jar`） | 需先 `mvn install` 安装 dsp-common/core/engine 到本地仓库 |

> 后端开发端口（**已验证**，直接读 `application.yml` 的 `server.port`）：
> - dsp-data-service → 8080（`dsp-data-service/src/main/resources/application.yml: 2`）
> - dsp-offline-service → 8081（`dsp-offline-service/src/main/resources/application.yml: 2`）
> - dsp-admin-service → 8082（`dsp-admin-service/src/main/resources/application.yml: 2`）

### 前端（dsp-admin-web）

| 命令 | 来源 | 标注 | 备注 |
|------|------|------|------|
| `npm run dev` | `dsp-admin-web/package.json: scripts.dev = "vite"` | 已验证 | dev server 端口 3000（`vite.config.js: server.port: 3000`） |

> **开发代理**（**已验证**，`dsp-admin-web/vite.config.js: 6-17`）：
> - `/dsp/admin` → `http://localhost:8082`（dsp-admin-service，`changeOrigin: true`）
> - `/dsp/offline` → `http://localhost:8081`（dsp-offline-service，`changeOrigin: true`）
>
> 注：前端未代理到 dsp-data-service（8080），数据查询 API `/dsp/api/{transno}` 在开发态无代理直连，需人工确认（推断：data-service 多为对外 API 网关，前端管理后台不直接消费）。

## 四、构建/打包

### 后端 Maven（dsp-parent）

| 命令 | 产物 | 来源 | 标注 |
|------|------|------|------|
| `mvn clean package` | 多模块 jar 集合 | `dsp-parent/pom.xml: <modules>` + 子模块 `pom.xml`；README/CLAUDE 给出此命令字面量 | 已验证（Maven 标准 package goal，父 POM packaging=pom） |
| `mvn clean package -DskipTests` | 同上，跳过测试 | `CLAUDE.md: 15`、`.github/workflows/ci.yml: 63`（CI "Package" 步骤字面量 `mvn clean package -DskipTests -f dsp-parent/pom.xml`） | 已验证（CI 配置中直接出现字面量） |
| `mvn clean compile` | 编译产物（无 jar） | `.github/workflows/ci.yml: 51`（CI "Build backend" 步骤字面量） | 已验证 |

> **可执行产物（fat-jar）与依赖产物（普通 jar）区分**（**已验证**，按 `pom.xml` 的 `spring-boot-maven-plugin` 存在性区分）：
>
> | 模块 | 产物类型 | 产物路径 | 配置依据 |
> |------|----------|----------|----------|
> | dsp-data-service | **fat-jar（可执行）** | `dsp-data-service/target/dsp-data-service-1.0.0.jar` | `dsp-data-service/pom.xml: 60-75` 含 `spring-boot-maven-plugin`（repackage） |
> | dsp-offline-service | **fat-jar（可执行）** | `dsp-offline-service/target/dsp-offline-service-1.0.0.jar` | `dsp-offline-service/pom.xml: 51-66` 含 `spring-boot-maven-plugin` |
> | dsp-admin-service | **fat-jar（可执行）** | `dsp-admin-service/target/dsp-admin-service-1.0.0.jar` | `dsp-admin-service/pom.xml: 63-78` 含 `spring-boot-maven-plugin` |
> | dsp-common | 普通 jar（依赖库） | `dsp-common/target/dsp-common-1.0.0.jar` | `dsp-common/pom.xml` **无** `spring-boot-maven-plugin`，无 `<build><plugins>` |
> | dsp-core | 普通 jar（依赖库） | `dsp-core/target/dsp-core-1.0.0.jar` | `dsp-core/pom.xml` **无** `spring-boot-maven-plugin`，无 `<build><plugins>` |
> | dsp-engine | 普通 jar（依赖库） | `dsp-engine/target/dsp-engine-1.0.0.jar` | `dsp-engine/pom.xml` **无** `spring-boot-maven-plugin`，无 `<build><plugins>` |
>
> 产物版本号 `1.0.0` 来自 `<dsp.version>1.0.0</dsp.version>`（`dsp-parent/pom.xml: 32`）+ 各子模块继承父 `version=1.0.0`。3 个可部署 jar 命名（`dsp-{name}-1.0.0.jar`）在 CI 启动步骤中字面量出现（`.github/workflows/ci.yml: 69`：`java -jar dsp-parent/dsp-data-service/target/dsp-data-service-1.0.0.jar`）。

### 前端 Vite（dsp-admin-web）

| 命令 | 产物 | 来源 | 标注 |
|------|------|------|------|
| `npm run build` | `dsp-admin-web/dist/` | `dsp-admin-web/package.json: scripts.build = "vite build"`；CI 字面量（`.github/workflows/ci.yml: 110`） | 已验证 |
| `npm run preview` | 预览 `dist/` | `dsp-admin-web/package.json: scripts.preview = "vite preview"` | 已验证 |

> `vite.config.js` 未显式配置 `build.outDir`，故产物走 Vite 默认 `dist/`（`dsp-admin-web/dist/` 实际存在，已构建）。产物为纯静态文件（HTML/JS/CSS），无后端嵌入。

## 五、部署方式

### 部署形态探测声明

| 部署形态 | 探测结果 | 配置文件 |
|----------|----------|----------|
| Dockerfile | **未发现** | —（探测 `**/Dockerfile*`、`**/*.dockerfile`、`docker/` 目录均无，排除 node_modules） |
| docker-compose | **未发现** | —（探测 `**/docker-compose*`、`**/compose*.yml`、`**/compose*.yaml` 均无） |
| Kubernetes | **未发现** | —（探测 `k8s/`、`deploy/`、`manifests/`、`helm/` 目录均无） |
| Makefile 部署 target | **未发现** | —（探测 `**/Makefile`、`**/*.mk` 均无） |
| 静态托管（前端） | **适用**（产物 `dist/` 为纯静态，无后端嵌入） | —（托管方式 Nginx/对象存储/CDN 未在配置中声明） |
| 反向代理配置 | **推断需要**（无配置文件） | 依据：`vite.config.js: 6-17` baseURL `/dsp/admin`、`/dsp/offline` 指向 8082/8081；生产无 `nginx.conf` / `Caddyfile` / `traefik.yml` |
| 部署脚本（.sh / deploy*） | **未发现**（仅 `test/api-test.sh` 测试脚本，非部署脚本） | — |

### 实际部署形态

项目**未发现任何容器化配置**（Dockerfile / docker-compose / k8s）与部署脚本/Makefile target。**部署形态为裸机/虚机直跑 fat-jar + 前端静态托管**（推断）。

| 部署步骤/方式 | 来源 | 标注 |
|---------------|------|------|
| 启动 dsp-data-service：`java -jar dsp-data-service/target/dsp-data-service-1.0.0.jar` | `README.md: 74`、`CLAUDE.md: 18`、CI 字面量 `.github/workflows/ci.yml: 69` | 已验证（CI 中直接出现该命令字面量） |
| 启动 dsp-offline-service：`java -jar dsp-offline-service/target/dsp-offline-service-1.0.0.jar` | `README.md: 77`、`CLAUDE.md: 19` | 推断（命令模式与 CI 中的 data-service 一致，offline-service 同结构，但 CI 未直接出现 offline 的启动命令字面量） |
| 启动 dsp-admin-service：`java -jar dsp-admin-service/target/dsp-admin-service-1.0.0.jar` | `README.md: 80`、`CLAUDE.md: 20` | 推断（同上，CI 未直接出现 admin 的启动命令字面量） |
| 前端部署：`dist/` 静态文件托管 | `dsp-admin-web/dist/` 存在（已构建产物）；`vite.config.js` 未声明托管目标 | 推断（依据：前端为纯静态产物；具体托管方式 Nginx/对象存储/CDN 未在配置中声明，需人工确认） |
| JVM 参数（-Xmx / GC 等） | 配置文件未声明 | 推断（依据：无任何 JVM 启动参数配置，CI 与文档均用裸 `java -jar`；生产 JVM 调优需人工确认） |
| 守护进程方式（systemd / nohup / supervisor） | CI 用 `nohup ... &`（`.github/workflows/ci.yml: 69`）；生产未声明 | 推断（CI 用 nohup 后台拉起仅用于测试环境；生产守护方式未在配置中声明，需人工确认） |
| 反向代理拓扑（前端→后端路由） | 配置文件无 nginx/网关配置 | 推断（依据：dev 由 Vite proxy 转发 `/dsp/admin`→8082、`/dsp/offline`→8081；生产需反向代理实现同样路由，但具体配置未在项目中声明，需人工确认） |
| 数据库初始化 | `dsp-parent/dsp-core/src/main/resources/db/init.sql`（建表脚本）；CI 字面量 `.github/workflows/ci.yml: 48` `mysql ... < dsp-parent/dsp-core/src/main/resources/db/init.sql` | 已验证 |

> 上述「推断」项均为配置未声明、需人工确认的部署细节，未与「已验证」事实混述。本节不含"建议加 Docker / 建议补 systemd unit"等顾问建议——配置里没有的东西只标注未发现或推断。

## 六、环境变量/配置项

> 三份 `application.yml` 大量使用 `${VAR:default}` 占位符，即为可外置配置项（**已验证**外置）。下表按服务列出。带默认值的敏感配置（密钥/密码）标注"生产必须替换"。

### dsp-data-service（`dsp-data-service/src/main/resources/application.yml`）

| 配置项（yml 路径 / 配置键） | 环境变量 | 默认值 | 来源文件 | 标注 |
|------------------------------|----------|--------|----------|------|
| `spring.datasource.dynamic.datasource.master.url`（DB 主机） | `DSP_DB_HOST` | localhost | `application.yml: 16` | 已验证 |
| `spring.datasource.dynamic.datasource.master.url`（DB 端口） | `DSP_DB_PORT` | 3306 | `application.yml: 16` | 已验证 |
| `spring.datasource.dynamic.datasource.master.url`（DB 名） | `DSP_DB_NAME` | dsp_config | `application.yml: 16` | 已验证 |
| `spring.datasource.dynamic.datasource.master.username` | `DSP_DB_USERNAME` | root | `application.yml: 17` | 已验证（**生产必须替换**） |
| `spring.datasource.dynamic.datasource.master.password` | `DSP_DB_PASSWORD` | root | `application.yml: 18` | 已验证（**生产必须替换** — 敏感） |
| `spring.redis.host` | `DSP_REDIS_HOST` | localhost | `application.yml: 38` | 已验证 |
| `spring.redis.port` | 未外置（写死） | 6379 | `application.yml: 39` | 已验证（写死，未做外置） |
| `spring.redis.password` | 未外置（写死） | （空） | `application.yml: 40` | 已验证（写死空值，生产需人工确认） |
| `spring.data.mongodb.uri` | `DSP_MONGO_URI` | mongodb://localhost:27017/dsp | `application.yml: 52` | 已验证 |
| `dsp.jwt.secret` | `DSP_JWT_SECRET` | dsp-platform-jwt-secret-key-must-be-at-least-32-characters-long-for-security | `application.yml: 68` | 已验证（**生产必须替换** — 敏感） |
| `dsp.jwt.expiration-hours` | `DSP_JWT_EXPIRATION_HOURS` | 24 | `application.yml: 69` | 已验证 |
| `dsp.security.encrypt-key` | `DSP_ENCRYPT_KEY` | DSP2026SecretKey4 | `application.yml: 72` | 已验证（**生产必须替换** — 敏感，用于数据源密码 AES 加密） |
| `server.port` | 未外置（写死） | 8080 | `application.yml: 2` | 已验证（写死，未做外置） |

### dsp-offline-service（`dsp-offline-service/src/main/resources/application.yml`）

| 配置项 | 环境变量 | 默认值 | 来源文件 | 标注 |
|-------|----------|--------|----------|------|
| DB url/host/port/name | `DSP_DB_HOST` / `DSP_DB_PORT` / `DSP_DB_NAME` | localhost / 3306 / dsp_config | `application.yml: 16` | 已验证 |
| DB username | `DSP_DB_USERNAME` | root | `application.yml: 17` | 已验证（**生产必须替换**） |
| DB password | `DSP_DB_PASSWORD` | root | `application.yml: 18` | 已验证（**生产必须替换** — 敏感） |
| Redis host | `DSP_REDIS_HOST` | localhost | `application.yml: 25` | 已验证 |
| Redis port | 未外置（写死） | 6379 | `application.yml: 26` | 已验证（写死） |
| Redis password | 未外置（写死） | （空） | `application.yml: 27` | 已验证（写死空值） |
| `dsp.jwt.secret` | `DSP_JWT_SECRET` | dsp-platform-jwt-secret-key-...（32+ chars） | `application.yml: 45` | 已验证（**生产必须替换** — 敏感） |
| `dsp.jwt.expiration-hours` | `DSP_JWT_EXPIRATION_HOURS` | 24 | `application.yml: 46` | 已验证 |
| `dsp.security.encrypt-key` | `DSP_ENCRYPT_KEY` | DSP2026SecretKey4 | `application.yml: 48` | 已验证（**生产必须替换** — 敏感） |
| `server.port` | 未外置（写死） | 8081 | `application.yml: 2` | 已验证（写死） |

> 注：dsp-offline-service **无 MongoDB 配置**（与 data-service/admin-service 不同，`application.yml` 全文无 `spring.data.mongodb`）。

### dsp-admin-service（`dsp-admin-service/src/main/resources/application.yml`）

| 配置项 | 环境变量 | 默认值 | 来源文件 | 标注 |
|-------|----------|--------|----------|------|
| DB url/host/port/name | `DSP_DB_HOST` / `DSP_DB_PORT` / `DSP_DB_NAME` | localhost / 3306 / dsp_config | `application.yml: 16` | 已验证 |
| DB username | `DSP_DB_USERNAME` | root | `application.yml: 17` | 已验证（**生产必须替换**） |
| DB password | `DSP_DB_PASSWORD` | root | `application.yml: 18` | 已验证（**生产必须替换** — 敏感） |
| Redis host | `DSP_REDIS_HOST` | localhost | `application.yml: 38` | 已验证 |
| Redis port | 未外置（写死） | 6379 | `application.yml: 39` | 已验证（写死） |
| Redis password | 未外置（写死） | （空） | `application.yml: 40` | 已验证（写死空值） |
| `dsp.jwt.secret` | `DSP_JWT_SECRET` | dsp-platform-jwt-secret-key-...（32+ chars） | `application.yml: 63` | 已验证（**生产必须替换** — 敏感） |
| `dsp.jwt.expiration-hours` | `DSP_JWT_EXPIRATION_HOURS` | 24 | `application.yml: 64` | 已验证 |
| `dsp.security.encrypt-key` | `DSP_ENCRYPT_KEY` | DSP2026SecretKey4 | `application.yml: 67` | 已验证（**生产必须替换** — 敏感） |
| `dsp.admin.username` | `DSP_ADMIN_USERNAME` | admin | `application.yml: 69` | 已验证（**生产必须替换** — 管理员账号） |
| `dsp.admin.password` | `DSP_ADMIN_PASSWORD` | admin123 | `application.yml: 70` | 已验证（**生产必须替换** — 敏感，管理员密码明文默认值） |
| `server.port` | 未外置（写死） | 8082 | `application.yml: 2` | 已验证（写死） |

> 注：dsp-admin-service **无 MongoDB 配置**（`application.yml` 全文无 `spring.data.mongodb`）。三服务的 DB/Redis/JWT/encrypt-key 共享同一组环境变量名（`DSP_DB_*` / `DSP_REDIS_HOST` / `DSP_JWT_SECRET` / `DSP_ENCRYPT_KEY`），生产部署时 3 个服务注入相同值。

### 前端（dsp-admin-web）

| 配置项 | 环境变量 | 默认值 | 来源文件 | 标注 |
|--------|----------|--------|----------|------|
| — | — | — | — | 未发现配置外置机制（`dsp-admin-web/` 无 `.env` / `.env.example` / `vite.config.js` 无 `env`/`define` 字段；API baseURL 在源码中，未做外置） |

> 前端 API 调用 baseURL 通过 Vite dev proxy 走相对路径 `/dsp/admin`、`/dsp/offline`（`vite.config.js: 8-16`），无独立环境变量外置机制，生产构建时 baseURL 需在源码或反向代理层处理（推断，需人工确认）。

## 七、CI/CD

### CI 配置探测声明

| CI 系统 | 探测结果 | 配置文件 |
|---------|----------|----------|
| GitHub Actions | **发现** | `.github/workflows/ci.yml` |
| GitLab CI | 未发现 | —（无 `.gitlab-ci.yml`） |
| Jenkins | 未发现 | —（无 `Jenkinsfile` / `jenkins/`） |
| CircleCI | 未发现 | —（无 `.circleci/config.yml`） |
| Travis | 未发现 | —（无 `.travis.yml`） |

### 现有流水线（GitHub Actions，`.github/workflows/ci.yml`）

**触发条件**（**已验证**，`ci.yml: 3-10`）：PR 到 `main` 分支，且变更路径命中 `dsp-parent/**` / `dsp-admin-web/**` / `test/**` / `.github/workflows/ci.yml`。并发控制 `cancel-in-progress: true`（`ci.yml: 12-14`）。

**两个 Job 并行**：

1. **backend（Backend Build & Test，`ci.yml: 17-88`）**，`runs-on: ubuntu-latest`，timeout 15min：
   - Services：MySQL 5.7 容器（`MYSQL_ROOT_PASSWORD: root`、`MYSQL_DATABASE: dsp_config`，端口 3306，带 healthcheck，`ci.yml: 22-34`）
   - `actions/setup-java@v4` JDK 8 temurin，`cache: maven`（`ci.yml: 40-45`）
   - 初始化数据库：`mysql -h 127.0.0.1 -u root -proot dsp_config < dsp-parent/dsp-core/src/main/resources/db/init.sql`（`ci.yml: 47-48`）
   - 构建：`mvn clean compile -f dsp-parent/pom.xml`（`ci.yml: 50-51`）
   - 测试：`mvn test -f dsp-parent/pom.xml`，注入 `DSP_DB_HOST/PORT/NAME/USERNAME/PASSWORD` 环境变量（`ci.yml: 53-60`）
   - 打包：`mvn clean package -DskipTests -f dsp-parent/pom.xml`（`ci.yml: 62-63`）
   - 启动 dsp-data-service：`nohup java -jar dsp-parent/dsp-data-service/target/dsp-data-service-1.0.0.jar &`，轮询 90s 等 8080 端口（`ci.yml: 65-85`）
   - API 集成测试：`bash test/api-test.sh`（`ci.yml: 87-88`）

2. **frontend（Frontend Build，`ci.yml: 90-111`）**，`runs-on: ubuntu-latest`，timeout 10min：
   - `actions/setup-node@v4` Node 20（`ci.yml: 99-102`）
   - 安装依赖：`working-directory: dsp-admin-web` → `npm install`（`ci.yml: 104-106`）
   - 构建：`working-directory: dsp-admin-web` → `npm run build`（`ci.yml: 108-110`）

**密钥/凭证管理**：CI **未使用 GitHub Secrets**（无 `${{ secrets.* }}` 引用），DB 凭证直接明文写在 `ci.yml` 的 services env 与 step env 中（`root/root`，仅测试环境）。

**部署步骤**：**无**——CI 流水线仅做 build + test + 产物（jar/dist），**未配置自动部署步骤**（无 deploy job / 无 push image / 无 ssh deploy）。

### 人工协作规范（推断线索）

项目有 `CONTRIBUTING.md` 描述人工协作流程（非自动化流水线）：分支策略（`main` 禁止 push 仅 PR、`feature/` / `hotfix/` / `release/` 分支）、Commit Message 格式（`<type>: <中文描述>`）、SemVer 版本管理（`CONTRIBUTING.md: 1-66`）。**无自动化 CD 流水线**，发布流程为人工：`release/vX.Y.Z` 分支 → 打 tag → 合并回 main（`CONTRIBUTING.md: 66`）。

> 本节只梳理 CI 现状，未补写"建议加 deploy job / 建议用 secrets 管理凭证 / 建议加 Docker 镜像推送"等顾问建议。

## 附：关键配置文件索引

| 用途 | 路径 |
|------|------|
| 后端父 POM（多模块 + 版本管理） | `dsp-parent/pom.xml` |
| dsp-common 子模块 POM（普通 jar，无构建插件） | `dsp-parent/dsp-common/pom.xml` |
| dsp-core 子模块 POM（普通 jar） | `dsp-parent/dsp-core/pom.xml` |
| dsp-engine 子模块 POM（普通 jar） | `dsp-parent/dsp-engine/pom.xml` |
| dsp-data-service POM（fat-jar，含 spring-boot-maven-plugin） | `dsp-parent/dsp-data-service/pom.xml` |
| dsp-offline-service POM（fat-jar） | `dsp-parent/dsp-offline-service/pom.xml` |
| dsp-admin-service POM（fat-jar） | `dsp-parent/dsp-admin-service/pom.xml` |
| dsp-data-service 配置 | `dsp-parent/dsp-data-service/src/main/resources/application.yml` |
| dsp-offline-service 配置 | `dsp-parent/dsp-offline-service/src/main/resources/application.yml` |
| dsp-admin-service 配置 | `dsp-parent/dsp-admin-service/src/main/resources/application.yml` |
| 数据库初始化脚本 | `dsp-parent/dsp-core/src/main/resources/db/init.sql` |
| 前端 package.json（scripts + deps） | `dsp-admin-web/package.json` |
| 前端 Vite 配置（dev 端口 + 代理） | `dsp-admin-web/vite.config.js` |
| 前端锁文件（npm） | `dsp-admin-web/package-lock.json` |
| CI 流水线 | `.github/workflows/ci.yml` |
| API 集成测试脚本 | `test/api-test.sh` |
