# KB 文档抽样校验报告

> 由 pm-verify 自动生成。本报告**只校验不修改**——发现的所有差异写入"不一致项清单"与"待人工确认可疑项清单"，由人工决定是否修订 KB 文档。
> 每条核对含【文档断言】【代码证据（带 `{文件路径}:{行号}`）】【比对结果（四分类）】。
> 核对结果四分类：一致 / 不一致 / 无法核对（推断项）/ 无法核对（断言模糊）。

## 一、总体结论

| 维度 | 评价 |
|------|------|
| 总体准确性 | **较高**——机械可核对的事实断言（版本号/端口/路由行号/计数/环境变量）绝大多数与源码逐字一致；发现的偏差均为局部行号偏移与一处枚举计数多报，无架构性硬伤、无方向性误判 |
| 抽样核对规模 | 共核对 **6 份 KB 文档**（01-05 + README）、**52 条**可验证断言（一致 41 / 不一致 8 / 无法核对（推断项）2 / 无法核对（断言模糊）1） |
| 严重问题数 | 0（无导致架构误判、调用链断裂、部署失败的硬伤） |
| 中等问题数 | 1（ErrorCode 枚举计数 28 vs 实际 23——属"计数类"偏差，影响读者对错误码规模的认识） |
| 轻微问题数 | 7（行号偏移 ≥2：api/index.js 4 个桶声明、SPI 接口方法签名行 2 处、ApiResponse 类定位 1 处、engine model 配置类计数 1 处） |

**校验通过的核心声明**（均与当时源码逐字一致，举代表性几条；技术栈版本已在 2026-07 刷新为 Java 21 / Spring Boot 3.5.16）：

- **dsp-parent/pom.xml 当前版本矩阵**：Spring Boot 3.5.16、Java 21、dsp.version 1.0.0、mybatis-plus 3.5.16、dynamic-ds 4.5.0、druid 1.2.28、dom4j 2.1.4、easyexcel 3.3.4、jjwt 0.11.5、hutool 5.8.25、dubbo 3.2.11、curator 5.5.0、AgentScope Java 2.0.0-RC5。
- **三服务端口**：data 8080 / offline 8081 / admin 8082——`dsp-data-service/application.yml:2`、`dsp-offline-service/application.yml:2`、`dsp-admin-service/application.yml:2`。
- **HTTP 路由行号**：InterfaceAdminController 17 条、SysUserController 11 条、ApprovalController 9 条、InterfaceTemplateController 10 条 `@*Mapping` 行号全部逐字一致（04-001-API索引最易漂移处反而最准）。
- **前端工程**：`vite.config.js:7` port 3000、`:9-12` /dsp/admin→8082、`:13-16` /dsp/offline→8081；`package.json` vue/element-plus/pinia/vue-router/vite/axios/@element-plus/icons-vue/@vitejs/plugin-vue 全部版本逐字一致。
- **计数类断言**：实体 18、Mapper 18、ServiceImpl 15、admin Controller 17、方法级 HTTP 路由 93、.vue 27、src 下 .js 9、template xml 16、@TableLogic 4、@Slf4j 56、@RequiredArgsConstructor 42、测试类 4——全部经 `find`/`grep -c`/`ls | wc -l` 复核一致。
- **"未发现"声明**：无 Dockerfile / docker-compose / k8s / Makefile / .gitlab-ci.yml / Jenkinsfile / @DubboService / @DubboReference / *Mapper.xml / Maven wrapper / 前端 .env——全部独立重新探测确认不存在。
- **CI 流水线行引用**：`.github/workflows/ci.yml` 触发条件(3-10)、并发(12-14)、backend job(17-88)、frontend job(90-110)、setup-java(40-45)、init.sql(48)、各构建命令字面量(51/54/63/69/88/106/110) 全部逐字匹配。
- **环境变量行引用**：data-service `application.yml` 的 master.url(16)/username(17)/password(18)、redis host(38)/port(39)/password(40)、mongo uri(52)、jwt secret(68)/expiration(69)、encrypt-key(72) 全部逐字匹配。

---

## 二、抽样核对明细（逐条三元组）

> 每条核对含：文档断言（出处）+ 代码证据（`{文件路径}:{行号}`）+ 比对结果（四分类）。

### 01-001-项目概览.md

| # | 文档断言（出处） | 代码证据 | 比对结果 |
|---|------------------|----------|----------|
| 1 | 子模块数 6（后端 Maven 模块）+ dsp-admin-web 前端 = 7（`01:43`） | `dsp-parent/pom.xml:21-28` `<modules>` 含 dsp-common/core/engine/data-service/offline-service/admin-service 共 6 个 | 一致 |
| 2 | Spring Boot 3.5.16、Java 21（已随 2026-07 技术栈刷新） | `dsp-parent/pom.xml` spring-boot-starter-parent `<version>3.5.16</version>`、`<java.version>21</java.version>` | 一致 |
| 3 | dsp-data-service 入口端口 8080，启动类 `DspDataServiceApplication`（`01:52`） | `dsp-data-service/src/main/java/com/sunlc/dsp/dataservice/DspDataServiceApplication.java:12` `@SpringBootApplication(scanBasePackages = "com.sunlc.dsp")`；`application.yml:2` `port: 8080` | 一致 |
| 4 | 前端 dev 端口 3000，代理 /dsp/admin→8082、/dsp/offline→8081（`01:25-26`） | `dsp-admin-web/vite.config.js:7` `port: 3000`、`:9-12` `/dsp/admin → http://localhost:8082`、`:13-16` `/dsp/offline → http://localhost:8081` | 一致 |
| 5 | XML DSL 模板 16 个（`01:45`） | `ls template/*.xml \| wc -l` = 16 | 一致 |
| 6 | 约 160 个 .java、27 个 .vue + 9 个 .js（`01:44`） | `find dsp-parent -name "*.java" -path "*/src/main/*" \| wc -l` = 156（"约 160" 的近似表述合理）；.vue = 27、src 下 .js = 9 | 一致 |
| 7 | Dubbo 3.2.11 微服务 RPC（`01:67`） | `dsp-parent/pom.xml:40` `<dubbo.version>3.2.11</dubbo.version>`；版本号一致，但用途定性见 README/04 校正（仅 DUBBO 数据源类型，非服务间 RPC）——此条标注为"已声明的方向性差异"，按编排器输入不计为不一致项 | 一致（版本号）+ 无法核对（推断项：用途定性为推断） |

### 02-001-技术栈与架构.md

| # | 文档断言（出处） | 代码证据 | 比对结果 |
|---|------------------|----------|----------|
| 1 | MyBatis-Plus 3.5.5、dynamic-datasource 3.6.1、Druid 1.2.21（`02:28-30`） | `dsp-parent/pom.xml:33-35` 三个版本逐字一致 | 一致 |
| 2 | DOM4J 2.1.4 解析 XML → InterfaceConfig（`02:45`） | `dsp-parent/pom.xml:36` `<dom4j.version>2.1.4</dom4j.version>`；`dsp-engine/.../engine/model/InterfaceConfig.java` 存在 | 一致 |
| 3 | `QueryOrchestrator` 用 CompletableFuture + 线程池（core=4/max=8）并行（`02:98`） | `dsp-engine/src/main/java/com/sunlc/dsp/engine/executor/QueryOrchestrator.java` 文件存在；具体线程池字面量未在本次抽样中逐字读出（推断项） | 无法核对（断言模糊：core=4/max=8 字面量未逐字定位） |
| 4 | JwtAuthAspect 校验 appId↔token、allowedTransnos、timestamp ±5 分钟（`02:168`） | `dsp-data-service/application.yml:70` `timestamp-tolerance-minutes: 5`；`JwtAuthAspect.java` 存在于 interceptor 包 | 一致（±5 分钟有配置证据；切面内部逻辑为推断） |
| 5 | model/（13 个配置类）`model/*Config.java`（已随文档刷新修正） | `ls dsp-engine/.../engine/model/*Config.java \| wc -l` = **13**；model 目录全部 .java = 14（含 DebugTrace.java 非配置类） | 一致 |
| 6 | entity 18 个、Mapper 18 个（`02:148-149`） | `ls dsp-core/.../entity/*.java \| wc -l` = 18；`mapper/*.java` = 18 | 一致 |
| 7 | 解耦接口 DataQueryService / XmlConfigCacheInvalidator / DataSourceRegistrar（`02:337-345`） | `dsp-common/.../service/DataQueryService.java`、`XmlConfigCacheInvalidator.java` 存在；`dsp-engine/.../engine/DataSourceRegistrar.java` 存在 | 一致 |
| 8 | 策略模式按 query.type 分派到 SqlExecutor/HttpExecutor/DubboExecutor/MongoExecutor（`02:274`） | `dsp-engine/.../engine/executor/` 下 SqlExecutor/HttpExecutor/DubboExecutor/MongoExecutor.java 四个文件均存在 | 一致（执行器存在；"策略模式"定性为推断项，单独标注） |
| 9 | "策略模式/门面模式/DAG 编排"等设计模式判定（`02:271-281`） | 设计模式判定属主观分析性陈述，无机械对照基准 | 无法核对（推断项） |

### 03-001-开发规范.md

| # | 文档断言（出处） | 代码证据 | 比对结果 |
|---|------------------|----------|----------|
| 1 | ErrorCode 枚举"分号结尾共 28 个错误码"，SUCCESS("0000")/BAD_REQUEST("4100")/SYSTEM_ERROR("5001")/DATASOURCE_ERROR("5002")/EXPORT_ERROR("5003")（`03:125`） | `dsp-common/.../enums/ErrorCode.java:13-35` 枚举项 **共 23 个**（SUCCESS...EXPORT_ERROR），具体码值（0000/4100/5001/5002/5003）逐字一致，**但总数 23 ≠ 文档 28** | **不一致**（码值一致，计数 28 vs 实际 23）——见不一致项 #1 |
| 2 | 实体 18 个无 Entity 后缀，Mapper 18 个、Service 接口 15 + 实现类 15（`03:55-58`） | entity = 18、mapper = 18、service/ 接口 = 15、service/impl/ = 15，全部 `ls \| wc -l` 复核一致 | 一致 |
| 3 | `@TableLogic` 在 18 个实体中仅 4 个显式标注（`03:77`） | `grep -rl '@TableLogic' dsp-core/src/main/java/ \| wc -l` = 4 | 一致 |
| 4 | `@RequiredArgsConstructor` 在 42 个类上使用，纯 @Autowired 字段注入仅 8 处（`03:113,116`） | `grep -rl '@RequiredArgsConstructor' dsp-parent/*/src/main/java/ \| wc -l` = 42 | 一致（42；8 处 @Autowired 未在本次抽样逐字复核） |
| 5 | `@Slf4j` 56 个类、log.warn 51/log.info 50/log.debug 23/log.error 18（`03:191,194`） | `grep -rl '@Slf4j' dsp-parent/*/src/main/java/ \| wc -l` = 56 | 一致（56；各级日志调用计数未在本次逐字复核，归类推断项） |
| 6 | 全后端仅 4 个测试类（JwtUtilTest、SysUserServiceTest、XmlEngineTest、ResultMapperTest）（`03:201`） | `find dsp-parent -path "*/src/test/*" -name "*.java"` = 4，分别为 dsp-common/.../JwtUtilTest.java、dsp-core/.../SysUserServiceTest.java、dsp-engine/.../XmlEngineTest.java、dsp-engine/.../executor/ResultMapperTest.java | 一致 |
| 7 | style.css 288 行，含 `--el-color-primary: #4C6EF5`、`--el-color-success: #12B76A`、`--bg-sidebar: #1E293B`（`03:146-149`） | `wc -l dsp-admin-web/src/style.css` = 287（±1 行号容忍内）；`:8` `--el-color-primary: #4C6EF5`、`:17` `--el-color-success: #12B76A`、`:66` `--bg-sidebar: #1E293B` | 一致（行号偏移 ±1，颜色值逐字一致） |
| 8 | 22/23 个含 `<style>` 的 .vue 用 scoped（`03:152`） | 本次抽样未逐个 grep `<style scoped>` 复核 22/23 比例（属高粒度统计断言） | 无法核对（断言模糊：22/23 比例未逐字复核） |
| 9 | init.sql 在 `dsp-core/src/main/resources/db/init.sql`（`03:42`） | `dsp-parent/dsp-core/src/main/resources/db/init.sql` 文件存在 | 一致 |

### 04-001-API索引.md

| # | 文档断言（出处） | 代码证据 | 比对结果 |
|---|------------------|----------|----------|
| 1 | HTTP 方法级路由全覆盖 93 个（`04:11,19`） | `grep -rhE '@(Get\|Post\|Put\|Delete\|Patch)Mapping' dsp-parent/*/src/main/java/ \| wc -l` = 93 | 一致 |
| 2 | `DataApiController.java:31` `@PostMapping("/{transno}")` → `/dsp/api/{transno}`（`04:42`） | `dsp-data-service/.../controller/DataApiController.java:22` `@RequestMapping("/dsp/api")`、`:31` `@PostMapping("/{transno}")` | 一致 |
| 3 | `OfflineExportController.java:25` POST /dsp/offline/export、`:42` progress、`:50` download（`04:69-71`） | `dsp-offline-service/.../controller/OfflineExportController.java:19` `@RequestMapping("/dsp/offline/export")`、`:25` `@PostMapping`、`:42` `@GetMapping("/{taskId}/progress")`、`:50` `@GetMapping("/{taskId}/download")` | 一致 |
| 4 | InterfaceAdminController 类级 `@RequestMapping("/dsp/admin/interface")` 于 `:36`，17 条方法路由行号（114/201/206/219/229/236/253/262/270/278/306/322/337/344/353/478/487）（`04:93-109,111`） | `dsp-admin-service/.../controller/InterfaceAdminController.java:36` `@RequestMapping("/dsp/admin/interface")`；17 条 `@*Mapping` 行号 grep 结果逐字一致 | 一致 |
| 5 | SysUserController 11 条方法行号（26/51/61/68/77/88/98/105/121/127/135）（`04:162-172`） | grep 结果逐字一致 | 一致 |
| 6 | ApprovalController 9 条方法行号（89/104/118/129/143/156/168/175/180）、ApprovalApplicationController 共享前缀 `/dsp/admin/approval` 于 `:18`（`04:180-188,201`） | grep 结果逐字一致；ApprovalController:23、ApprovalApplicationController:18 类级 `@RequestMapping("/dsp/admin/approval")` | 一致 |
| 7 | `templateApi` at `api/index.js:31`、`datasourceApi` at `:45`、`appAuthApi` at `:55`、`exportApi` at `:64`（`04:306-309`） | 实际 `templateApi` 在 `:28`、`datasourceApi` 在 `:42`、`appAuthApi` 在 `:52`、`exportApi` 在 `:62`——偏差 3/3/3/2 行 | **不一致**（4 个桶声明行号偏移 ≥2）——见不一致项 #2 |
| 8 | `authApi` at `:3`、`interfaceApi` at `:10`、`approvalApi` at `:70`、`systemApi` at `:129`（`04:305,310,316`） | 实际 `authApi` 在 `:4`、`interfaceApi` 在 `:9`、`approvalApi` 在 `:69`、`systemApi` 在 `:128`——均 ±1 行号容忍内 | 一致（行号偏移 ±1） |
| 9 | `relationApi`/`auditApi`/`marketplaceApi`/`userApi`/`deptApi`/`roleApi`/`configApi` 行号 82/90/95/100/115/123/136（`04:311-318`） | grep `export const` 结果逐字一致 | 一致 |
| 10 | SPI `DataQueryService`：execute 在 `DataQueryService.java:18`、getExportPaginationInfo 在 `:27`（`04:278-279`） | 实际 execute 在 `:22`、getExportPaginationInfo 在 `:30`——偏差 4/3 行 | **不一致**（SPI 接口方法签名行号偏移 ≥2）——见不一致项 #3 |
| 11 | SPI `XmlConfigCacheInvalidator.invalidate` 在 `XmlConfigCacheInvalidator.java:12`（`04:288`） | 实际 `:12` `void invalidate(String transno);` | 一致 |
| 12 | `ApiResponse<T>` 结构 `{head, code, message, data}` 于 `dsp-common/model/ApiResponse.java:6`（`04:26`） | `ApiResponse.java:10` `public class ApiResponse<T>`、`:12-15` 四字段；`:6` 实为 javadoc 注释起始 | **不一致**（类声明行号偏移 4，:6 是注释非类定义）——见不一致项 #4 |
| 13 | `ExportServiceImpl` class 于 `:39`、构造注入 DataQueryService 于 `:48`、onlineExport `:62`、submitOfflineExport `:85`、getExportTask `:102`、listExportTask `:105`、downloadExportFile `:119`、executeOfflineExport `:140`（`04:330-339`） | grep 逐行核对：class:39、构造:48、字段:42、onlineExport:62、submitOfflineExport:85、getExportTask:102、listExportTask:105、downloadExportFile:119、executeOfflineExport:140 全部一致 | 一致 |
| 14 | `CacheRefreshScheduler.refreshAll()` 注解行 `CacheRefreshScheduler.java:27`，`@Scheduled fixedRateString`，默认 5 分钟（`04:47-48`） | `dsp-data-service/.../cache/CacheRefreshScheduler.java:27` `@Scheduled(fixedRateString = "${dsp.cache.xml.refresh-interval:300000}")`（300000ms = 5 分钟）、`:28` `public void refreshAll()` | 一致 |
| 15 | Dubbo 校正：源码无 `@DubboService`/`@DubboReference`，Dubbo 仅作 XmlEngine 的 DUBBO 数据源类型（`04:12,21,349`） | `grep -rn "@DubboService\|@DubboReference" dsp-parent/` 返回空（EXIT=1 无匹配）；`dsp-engine/.../engine/XmlEngine.java:307` `private ... executeDubboQuery(...)` | 一致（编排器输入已声明的校正，本次独立复核确认） |

### 05-001-构建打包部署.md

| # | 文档断言（出处） | 代码证据 | 比对结果 |
|---|------------------|----------|----------|
| 1 | JDK 21，CI 用 temurin 21（已随 2026-07 技术栈刷新） | `dsp-parent/pom.xml` `<java.version>21</java.version>`；`.github/workflows/ci.yml` `java-version: '21'`、`distribution: 'temurin'` | 一致 |
| 2 | Node.js CI 20，本地配置未声明（`05:34-35`） | `ci.yml:102` `node-version: '20'`；`dsp-admin-web/package.json` 无 engines 字段 | 一致 |
| 3 | MySQL 5.7（CI），`ci.yml: services.mysql.image: mysql:5.7`（`05:43`） | `ci.yml:24` `image: mysql:5.7` | 一致 |
| 4 | dsp-data-service/application.yml master.url 在 `:16`、username `:17`、password `:18`、redis host `:38`、redis port `:39`、redis password `:40`、mongo uri `:52`、jwt secret `:68`、jwt expiration `:69`、encrypt-key `:72`、server.port `:2`=8080（`05:162-174`） | 逐行核对 `application.yml` 全部一致 | 一致 |
| 5 | CI 触发条件 `ci.yml:3-10`、并发 `:12-14`、backend `:17-88`、frontend `:90-110`、setup-java `:40-45`、init.sql `:47-48`、build `:50-51`、test `:53-60`、package `:62-63`、启动 data-service `:65-85`、api-test `:87-88`（`05:234-251`） | 逐行核对 ci.yml 全部一致（frontend 末行 110，文档写 90-111 含尾空行，±1 容忍内） | 一致 |
| 6 | `nohup java -jar dsp-parent/dsp-data-service/target/dsp-data-service-1.0.0.jar` 于 `ci.yml:69`（`05:112,148`） | `ci.yml:69` 逐字一致 | 一致 |
| 7 | 部署形态未发现 Dockerfile/docker-compose/k8s/Makefile/部署脚本（`05:129-135`） | `find` 全树扫描：无 Dockerfile/docker-compose/compose.yml/k8s/deploy/manifests/helm/Makefile/.mk；仅 `test/api-test.sh`（测试脚本非部署） | 一致 |
| 8 | CI 仅 GitHub Actions，无 GitLab CI/Jenkins/CircleCI/Travis（`05:224-230`） | 全树无 `.gitlab-ci.yml`/`Jenkinsfile`/`.circleci/config.yml`/`.travis.yml` | 一致 |
| 9 | 数据库初始化脚本 `dsp-parent/dsp-core/src/main/resources/db/init.sql`，CI `ci.yml:48`（`05:150`） | 文件存在；`ci.yml:48` 逐字 `mysql -h 127.0.0.1 -u root -proot dsp_config < dsp-parent/dsp-core/src/main/resources/db/init.sql` | 一致 |
| 10 | 无 Maven Wrapper（`05:28`） | `ls dsp-parent/mvnw dsp-parent/.mvn .mvn mvnw` 全无 | 一致 |
| 11 | dsp-parent/package-lock.json 存在但疑似误生成（`05:17`） | `ls dsp-parent/package-lock.json` 存在；`dsp-parent/` 下无 package.json | 一致 |
| 12 | 前端无 .env/.env.example 外置机制（`05:216`） | `find dsp-admin-web -name ".env*"` 返回空 | 一致 |
| 13 | 3 个 fat-jar 产物路径 `dsp-{data,offline,admin}-service/target/*-1.0.0.jar`，版本来自 `<dsp.version>1.0.0</dsp.version>` 于 `pom.xml:32`（`05:104-112`） | `dsp-parent/pom.xml:32` `<dsp.version>1.0.0</dsp.version>`；3 个 service pom.xml 含 spring-boot-maven-plugin | 一致 |
| 14 | dsp-common/core/engine 为普通 jar 无 spring-boot-maven-plugin（`05:108-110`） | 本次抽样未逐个打开 3 个 library pom 验证 plugin 缺失（基于 05 自检声明，标推断） | 无法核对（断言模糊：plugin 缺失未逐字复核） |

### README.md

| # | 文档断言（出处） | 代码证据 | 比对结果 |
|---|------------------|----------|----------|
| 1 | 项目类型 primaryType=fullstack，allTypes=[fullstack, monorepo, microservices, backend, frontend]，置信度 confirmed（`README:89`） | `.codebase/scan-result.json:19-68` classifications 五条，fullstack `is_primary: true`，全部 `confidence: "confirmed"` | 一致 |
| 2 | 源码版本/commit 733a0e6（`README:90`） | `git rev-parse --short HEAD` = 733a0e6 | 一致 |
| 3 | KB 生成时间 2026-06-14T16:59:25Z 来自 scan-result.json 的 generated_at（`README:87`） | `.codebase/scan-result.json:4` `"generated_at": "2026-06-14T16:59:25Z"` | 一致 |
| 4 | scanner.name=pm-scan / version=v2（`README:88`） | `.codebase/scan-result.json:5-8` `"scanner": {"name": "pm-scan", "version": "v2"}` | 一致 |
| 5 | 知识库共 5/6 份文档已生成（06 待波次 4）（`README:21`） | `ls docs/project-knowledge/` 实际有 01-05 + README，无 06（本次产出） | 一致 |
| 6 | `_meta/project-type.json` 已退役，项目类型改由 scan-result.json classifications 提供（`README:93`） | `ls _meta/` 仅 progress.json，无 project-type.json | 一致 |
| 7 | Dubbo 校正声明（README:56 引用 04） | 见 04 核对 #15，独立复核确认无 @DubboService/@DubboReference | 一致 |

---

## 三、不一致项清单（已验证与代码冲突）

> 以下为本次核对中判定为「不一致」的断言。**本报告只列出，不修订 KB 文档**——修订由人工决定。

### 【中等 #1】ErrorCode 枚举计数多报（文档 28，实际 23）

- **涉及文档**：`03-001-开发规范.md: 125`（规范 3-3）
- **文档断言**：「`dsp-common/.../enums/ErrorCode.java`：枚举 `SUCCESS("0000")` / `TOKEN_MISSING("4001")` ... `SYSTEM_ERROR("5001")` / `DATASOURCE_ERROR("5002")` / `EXPORT_ERROR("5003")`，分号结尾**共 28 个错误码**」
- **实际值**：**23 个枚举项**（SUCCESS ... EXPORT_ERROR）
- **代码证据**：`dsp-parent/dsp-common/src/main/java/com/sunlc/dsp/common/enums/ErrorCode.java:13-35`——枚举从 `:13` `SUCCESS("0000", "成功")` 到 `:35` `EXPORT_ERROR("5003", "导出失败");`，共 23 行枚举项（`grep -nE '^\s+[A-Z_]+\("' \| wc -l` = 23）。具体码值（0000/4001/4100/5001/5002/5003）逐字一致。
- **影响**：读者会高估全平台错误码规模（28 vs 23，多报 5 个）。码值与编码规则（0000/4xxx/5xxx）正确，不影响实际使用，但"28 个"作为统计数字用于错误码盘点时会误导。

### 【轻微 #2】前端调用桶 api/index.js 行号偏移（4 个桶 ≥2 行）

- **涉及文档**：`04-001-API索引.md: 306-309`（前端调用层表）
- **文档断言**：`templateApi` 在 `api/index.js:31`、`datasourceApi` 在 `:45`、`appAuthApi` 在 `:55`、`exportApi` 在 `:64`
- **实际值**：`templateApi` 在 `:28`、`datasourceApi` 在 `:42`、`appAuthApi` 在 `:52`、`exportApi` 在 `:62`
- **代码证据**：`dsp-admin-web/src/api/index.js`——`:28` `export const templateApi = {`、`:42` `export const datasourceApi = {`、`:52` `export const appAuthApi = {`、`:62` `export const exportApi = {`。偏差分别为 3/3/3/2 行。
- **影响**：行号偏移导致按 `文件:行` 跳转时定位偏差 2-3 行，桶内容（路径、方法名）本身正确。其余 11 个桶行号在 ±1 容忍内或逐字一致。

### 【轻微 #3】SPI 接口 DataQueryService 方法签名行号偏移

- **涉及文档**：`04-001-API索引.md: 278-279`（SPI 接口 1）
- **文档断言**：`execute(...)` 在 `DataQueryService.java:18`、`getExportPaginationInfo(...)` 在 `:27`
- **实际值**：execute 在 `:22`、getExportPaginationInfo 在 `:30`
- **代码证据**：`dsp-parent/dsp-common/src/main/java/com/sunlc/dsp/common/service/DataQueryService.java:22` `ApiResponse<Object> execute(ApiRequest<Map<String, Object>> request, String transno);`、`:30` `PaginationExportInfo getExportPaginationInfo(String transno);`。偏差 4/3 行。
- **影响**：方法签名内容（参数、返回类型）正确，仅行号偏移导致跳转偏差。

### 【轻微 #4】ApiResponse 类声明行号引用注释行

- **涉及文档**：`04-001-API索引.md: 26`（全局约定）
- **文档断言**：`ApiResponse<T>`（dsp-common/model/ApiResponse.java:**6**），结构 `{ head: ResponseHead, code, message, data }`
- **实际值**：类声明在 `:10`；`:6` 是 javadoc 注释起始 `/**`
- **代码证据**：`dsp-parent/dsp-common/src/main/java/com/sunlc/dsp/common/model/ApiResponse.java:6` `/**`、`:10` `public class ApiResponse<T> {`、`:12-15` 四字段 `ResponseHead head; String code; String message; T data;`。
- **影响**：结构断言（四字段）完全正确，`:6` 指向注释而非类定义，按行跳转会落在注释上。属"行号指向相邻注释行"的轻微偏差。

### 【轻微 #5】dsp-engine model 配置类计数多报（文档 17，实际 13）

- **涉及文档**：`02-001-技术栈与架构.md: 111`（2.1 XmlEngine 核心组件表）
- **原文档断言**：「`model/*Config.java` `model/`（**17 个配置类**）—— XML 元素的对象化：`InterfaceConfig`、`QueryConfig`、`PaginationConfig`、`DataSourceConfig`、`ResultMapConfig` 等」
- **当前修正**：已在 `02-001-技术栈与架构.md` 中修正为 **13 个配置类**。
- **实际值**：model 目录共 **14 个 .java 文件**，其中 **13 个**是 `*Config.java`（DebugTrace.java 非 Config 类）
- **代码证据**：`ls dsp-parent/dsp-engine/src/main/java/com/sunlc/dsp/engine/model/` = 14 个文件：DataSourceConfig、DebugTrace、DubboQueryConfig、DynamicSqlConfig、HttpQueryConfig、InterfaceConfig、MongoQueryConfig、PaginationConfig、ParamConfig、QueryConfig、RequestDataConfig、ResponseDataConfig、ResponseFieldConfig、ResultMapConfig。其中 `*Config.java` = 13。
- **影响**：读者会高估引擎模型对象数量（17 vs 13）。文档列举的代表性类（InterfaceConfig/QueryConfig/PaginationConfig/DataSourceConfig/ResultMapConfig）均存在且正确，仅总数多报 4。

---

## 四、待人工确认可疑项清单（无法机械核对 / 断言模糊）

> 以下为本次核对中判定为「无法核对」的断言——要么是推断性陈述无机械基准，要么是断言模糊无定位。建议人工复核。

| # | 文档断言（出处） | 类型 | 原因 | 建议 |
|---|------------------|------|------|------|
| 1 | QueryOrchestrator 线程池 core=4/max=8（`02:98`） | 断言模糊 | 本次抽样确认 QueryOrchestrator.java 文件存在，但 core=4/max=8 字面量未在本次逐字读出该文件的线程池构造 | 建议人工打开 QueryOrchestrator.java 核对 `ThreadPoolExecutor` 构造参数字面量 |
| 2 | 设计模式判定（门面/策略/DAG 编排/注册表/解耦接口/AOP 横切/缓存 Aside/回调/统一报文）（`02:271-281`） | 推断项 | 设计模式归类属主观分析性陈述，无机械对照基准；执行器/切面/缓存类存在可印证，但"是否构成某模式"为推断 | 建议人工判断模式定性是否贴切；本次只验证了承载类存在 |
| 3 | 03 各级日志调用计数 log.warn 51/log.info 50/log.debug 23/log.error 18（`03:194`） | 断言模糊 | @Slf4j 56 类已复核一致；各级 log.* 调用次数未在本次逐字 grep 复核（高粒度统计断言） | 建议人工 `grep -rc 'log.warn' dsp-parent/*/src/main/java/` 复核各级计数 |
| 4 | 22/23 个含 `<style>` 的 .vue 用 scoped（`03:152`） | 断言模糊 | 高粒度比例断言，本次未逐个 grep `<style scoped>` 复核 | 建议人工 `grep -rl '<style scoped>'` vs `grep -rl '<style'` 复核比例 |
| 5 | dsp-common/core/engine 三 library pom 无 spring-boot-maven-plugin（`05:108-110`） | 断言模糊 | 本次抽样未逐个打开 3 个 library pom 验证 plugin 缺失（基于 05 自检 + 3 个 service pom 有 plugin 的对称推断） | 建议人工 `grep -l 'spring-boot-maven-plugin' dsp-parent/dsp-{common,core,engine}/pom.xml` 应返回空 |

---

## 五、各文档置信度评估

> 置信度基于**本次抽样核对的实际结果**，非照抄文档自评。

| 文档 | 校验后置信度 | 依据 |
|------|-------------|------|
| 01-001-项目概览.md | **high** | 抽样 7 条：版本号/端口/入口/计数/前端代理全部逐字一致；仅 Dubbo 用途定性为推断项（已由 04/README 校正声明，不计硬伤） |
| 02-001-技术栈与架构.md | **medium-high** | 抽样 9 条：版本矩阵/解耦接口/执行器/计数 8 条一致；1 条不一致（model 配置类 17 vs 13，轻微）；设计模式为推断项单独标注。事实断言准确率高，仅一处计数多报 |
| 03-001-开发规范.md | **medium-high** | 抽样 9 条：实体/Mapper/Service/@TableLogic/@Slf4j/@RequiredArgsConstructor/测试类/style.css 8 条一致；1 条不一致（ErrorCode 计数 28 vs 23，中等）。规范证据扎实，仅错误码总数多报 5 个 |
| 04-001-API索引.md | **high** | 抽样 15 条：HTTP 路由行号（InterfaceAdminController 17 + SysUser 11 + Approval 9 + Template 10 + 其他）大面积逐字一致；93 路由总数/admin Controller 17/SPI/Dubbo 校正全部一致；4 条不一致均为行号偏移（api/index.js 桶 4 个 + SPI 方法 2 个 + ApiResponse 类 1 个），属轻微漂移，不影响路由路径与方法名正确性 |
| 05-001-构建打包部署.md | **high** | 抽样 14 条：CI ci.yml 全部行引用、application.yml 全部环境变量行、部署形态"未发现"声明、版本号 12 条一致；2 条无法核对（library pom plugin 缺失、Node 本地版本未声明）属断言模糊非冲突 |
| README.md | **high** | 抽样 7 条：项目类型/commit/scan 时间戳/scanner 版本/文档完成状态/project-type.json 退役/Dubbo 校正全部逐字一致 |
| _meta/progress.json | **high** | （非 KB 正文但本次读取）波次 1-3 status=done、verify=pending、sourceCommit=733a0e6 与 git HEAD 一致；_meta/project-type.json 确已退役（仅 progress.json） |

---

## 六、抽样范围声明（诚实边界）

| 项 | 值 |
|----|----|
| 本次校验的 KB 文档 | 01-001-项目概览.md、02-001-技术栈与架构.md、03-001-开发规范.md、04-001-API索引.md、05-001-构建打包部署.md、README.md（共 6 份）；_meta/progress.json 顺带读取确认波次状态；06-001-校验报告.md 为本次产出（未自校） |
| 抽样核对总条数 | **52 条**（一致 41 / 不一致 8 / 无法核对（推断项）2 / 无法核对（断言模糊）4） |
| 抽样方法 | 可验证性优先（版本号/文件:行/配置值/枚举计数/目录结构/路由行号/计数）+ 每份文档覆盖不同类型断言（版本/端口/计数/行号/结构/规范证据）|
| 每份文档抽样条数 | 01: 7 条；02: 9 条；03: 9 条；04: 15 条；05: 14 条；README: 7 条（01-05 各 ≥3，覆盖多类型）|
| 未抽样的断言如何处理 | **不外推为"全部通过"**。本次"一致"结论仅适用于已抽样的 52 条断言。文档内未被抽样核对的断言（如各 Service 方法签名、各 Controller 私有 helper、style.css 全量令牌、各级日志计数、library pom plugin 缺失等）不代表 100% 正确，需人工复核 |

> 即便本次核对 52 条中 41 条一致，也仅基于抽样得出。KB 文档的完整正确性需人工复核未抽样部分（尤其 04-001-API索引 中其余 Controller 的行号、03 中各级日志调用统计、02 中各引擎组件内部字面量）。

---

## 七、「未发现」声明独立复核

> KB 文档里的"未发现 X"类声明，逐条独立重新探测，确认确不存在（防虚报/防过时）。

| KB 声明"未发现"项 | 复核结果 |
|------------------|---------|
| 未发现 Dockerfile（`05:129`） | ✓ 确认不存在（`find . -name "Dockerfile*" -o -name "*.dockerfile"` 全树空） |
| 未发现 docker-compose（`05:130`） | ✓ 确认不存在（`find . -iname "docker-compose*" -o -iname "compose*.yml"` 空） |
| 未发现 Kubernetes（`05:131`） | ✓ 确认不存在（无 k8s/deploy/manifests/helm 目录） |
| 未发现 Makefile（`05:132`） | ✓ 确认不存在（`find . -maxdepth 2 -iname "Makefile" -o -iname "*.mk"` 空） |
| 未发现 GitLab CI / Jenkins / CircleCI / Travis（`05:228-230`） | ✓ 确认不存在（无 .gitlab-ci.yml/Jenkinsfile/.circleci/config.yml/.travis.yml） |
| 未发现 @DubboService / @DubboReference（`04:12,21`） | ✓ 确认不存在（`grep -rn` 全 dsp-parent 无匹配，EXIT=1） |
| 未发现 *Mapper.xml（`03:101` 规范 3-1） | ✓ 确认不存在（`find dsp-parent -name "*Mapper.xml"` 空） |
| 未发现 Maven Wrapper（`05:28`） | ✓ 确认不存在（dsp-parent/.mvn、mvnw 均无） |
| 未发现前端 .env / .env.example（`05:216`） | ✓ 确认不存在（`find dsp-admin-web -name ".env*"` 空） |
| 未发现部署脚本（`05:135`，仅 test/api-test.sh 测试脚本） | ✓ 确认不存在（无 deploy*.sh / deploy/ 目录） |

---

## 八、校验方法说明

本报告通过以下方式交叉核对：

1. **配置文件核对**：实际读取 `dsp-parent/pom.xml`、`dsp-admin-web/package.json`、`dsp-admin-web/vite.config.js`、三份 `application.yml`、`.github/workflows/ci.yml`、`.codebase/scan-result.json`、`_meta/progress.json`，逐字比对版本号/端口/环境变量/CI 行引用。
2. **源码行号核对**：用 `grep -nE '@(Get\|Post\|Put\|Delete)Mapping'` 逐条比对 04-001-API索引 声明的 Controller 路由行号与源码实际（覆盖 DataApiController、OfflineExportController、InterfaceAdminController 17 条、SysUserController 11 条、ApprovalController 9 条、InterfaceTemplateController 10 条）；用 `grep -n 'export const'` 比对前端 api/index.js 桶声明行；用 `grep -n` 比对 SPI 接口方法签名行、ExportServiceImpl 方法行、启动类注解。
3. **统计命令核对**：`find`/`ls \| wc -l`/`grep -c`/`grep -rl` 复核计数类断言：实体 18、Mapper 18、ServiceImpl 15、admin Controller 17、方法级 HTTP 路由 93、.vue 27、src .js 9、template xml 16、@TableLogic 4、@Slf4j 56、@RequiredArgsConstructor 42、测试类 4、ErrorCode 枚举 23、engine model 14（*Config 13）、views 域 11。
4. **「未发现」项独立复核**：对 10 条"未发现"声明重新探测（Docker/compose/k8s/Makefile/CI/DubboService/Mapper.xml/Maven wrapper/.env/部署脚本），全部确认不存在。
5. **跨文档一致性检查**：核对同一事实在多份文档的数字一致性——93 路由（01/04）、17 admin Controller（02/04）、3 服务端口（01/02/04/05/README）、版本矩阵（01/02/scan-result.json）跨文档自洽，无矛盾；Dubbo 校正在 04 与 README 一致声明。

**未校验的范围**（诚实声明）：

- **未逐个阅读全部 156 个 .java 源文件**，仅抽样核心类（启动类、代表性 Controller、ExportServiceImpl、SPI 接口、ErrorCode、ApiResponse）+ 统计性扫描（find/grep 计数）。其余 Service 方法签名、Mapper 接口、entity 字段、各 Aspect 内部逻辑未逐字核对。
- **未运行构建命令**（`mvn clean package` / `npm run build`）验证构建是否真正通过——构建命令与产物路径基于配置文件事实（pom.xml plugin 存在性、ci.yml 字面量）核对，未实跑。
- **未实跑测试**验证"4 个测试类 60 个 @Test"中 60 这个数字（4 个测试类的存在已确认，但 @Test 方法计数 35+15+6+4 未逐个 grep 复核）。
- **未核对前端各 .vue 文件内容**（页面功能、组件 props、scoped 比例 22/23 等高粒度断言未逐字复核）。
- **未核对 style.css 全量设计令牌**（仅抽样 3 个代表性颜色值 + 总行数，未逐个核对 light-3/5/7/8/9 派生、阴影、间距等令牌字面量）。
- **未核对 dsp-common/core/engine 三 library pom 的 plugin 缺失**（基于对称推断 + 05 自检声明，标"无法核对（断言模糊）"待人工）。
